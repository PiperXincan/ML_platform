# 一体化机器学习训练平台（桌面 Web 前端）开发文档 V7

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | ML Studio 一体化机器学习训练平台 |
| 当前版本 | 前端 V7 |
| 目标用户 | 了解少量机器学习知识、希望通过零代码训练模型的初学者 |
| 支持任务 | 二分类、普通回归 |
| 运行端 | 桌面 Web，最低宽度 1280px |
| 数据来源 | CSV 文件、内置示例数据 |
| 实现边界 | 纯前端演示，数据分析、训练、调参、报告和 API 均为稳定 Mock |
| 需求依据 | `frontend-change-requirements-v7.md` |

## 2. 产品目标

1. 通过项目制管理数据集、模型实验和参数结果。
2. 让初学者通过固定步骤完成数据检查、特征工程、模型训练和结果理解。
3. 在关键指标旁提供通俗解释、参考范围和优化方向。
4. 降低错误配置概率，不向用户暴露不适用于当前任务或模型的选项。
5. 为未来接入真实数据分析、训练、模型服务和 API 网关预留服务层边界。

## 3. MVP 边界

### 3.1 当前实现

- 项目、数据集、模型实验和参数结果的本地管理。
- CSV 类型检查、数据预览和目标列配置。
- 特征准备，以及模型级缺失值处理、标准化和参数配置。
- 二分类与回归模型选择、模拟训练和模拟调参。
- Top 3 训练结果、全部结果、模型报告详情、模型库和模拟 API 说明。
- LocalStorage 持久化和稳定 Mock 数据。

### 3.2 当前不实现

- 真实数据清洗、特征工程、模型训练、自动调参和预测服务。
- 多分类、时序预测、聚类、深度学习和 AutoML。
- 登录、权限、团队协作、计费、云端存储和数据集版本管理。
- Excel、数据库和云端数据源。
- 真实模型部署、API 密钥、调用额度、调用统计和服务启停。
- 移动端和平板适配。

## 4. 已确认的产品规则

### 4.1 项目层级

平台使用以下层级：

`项目 → 数据集 → 模型实验 → 参数结果`

- 一个项目可以包含多个数据集。
- 一个数据集可以包含多个模型实验。
- 一个模型实验对应一个模型类型和多组参数结果。
- 同一模型类型允许创建多个实验。

### 4.2 任务类型

- 创建项目时选择二分类或回归，并自动记录创建时间。
- 项目未上传数据时允许修改任务类型。
- 上传数据或产生训练记录后不允许修改任务类型。
- 分类项目只展示分类模型，回归项目只展示回归模型。
- 时序预测未来独立建设，当前不提供入口。

### 4.3 目标列

- 目标列属于数据集，一个数据集只设置一个目标列。
- 同一数据集下所有模型实验共享目标列。
- 特征工程或训练开始后修改目标列，将清空特征配置并使已有结果失效。
- 需要预测其他目标时，用户应创建新数据集。

## 5. 信息架构

### 5.1 全局导航

| 一级入口 | 主要内容 |
| --- | --- |
| 首页 | 产品引导、最近项目、创建项目 |
| 项目 | 项目列表、数据集悬浮弹窗、项目详情 |
| 模型库 | 按项目分组的最佳模型、筛选和 API 入口 |

项目内部使用二级导航：

`项目概览 → 数据集 → 模型实验`

数据集内部使用固定步骤条：

`数据检查 → 特征准备 → 模型训练 → 训练结果`

- 侧边栏只保留首页、项目、模型库和初学者模式提示，不展示项目快捷列表。
- 二级页面按实际去向显示返回按钮；一级页面不显示“返回上一步”。
- 流程页面使用底部吸附操作栏：返回按钮固定在左侧，下一步或主要操作固定在右侧，页面未滚动到底部时也保持可见。
- 项目详情、模型实验列表、报告详情和 API 说明等二级页面同样使用该底部操作栏，避免返回按钮位置变化。
- 模型报告不属于流程步骤，是训练结果的下级详情页。

### 5.2 步骤状态

| 状态 | 说明 |
| --- | --- |
| 未开始 | 尚未进入或缺少前置配置 |
| 进行中 | 已进入但配置尚未完成 |
| 已完成 | 当前配置有效，可以进入下一步 |
| 历史结果 | 上游配置已变化，旧结果仅供查看 |

修改目标列、字段类型、数据划分或共享特征集合后，已有成功实验标记为“历史结果”，不删除旧结果，也不允许再次训练。

## 6. 页面设计

### 6.1 首页

- 展示产品定位和“创建项目”主按钮。
- 展示最近项目卡片，包括项目名称、任务类型、数据集数量、模型实验数量和更新时间。
- 无项目时提供二分类和回归示例入口。

### 6.2 创建项目

只包含以下字段：

| 字段 | 规则 |
| --- | --- |
| 项目名称 | 必填，本地项目内唯一 |
| 任务类型 | 二分类或回归，必填 |
| 创建时间 | 系统生成，不可编辑 |

创建成功后进入项目详情空状态，引导用户上传第一个数据集。

### 6.3 数据集悬浮弹窗

- 项目名称不显示悬浮弹窗。
- 鼠标停留数据集卡片约 300ms 后打开弹窗。
- 弹窗包含“数据概览”和“数据预览”两个分页。
- 数据概览使用统计表展示行列数、目标列、缺失数量，以及前 50 个特征的统计指标；每页展示 10 个特征。
- 统计表按字段类型展示适用指标，不适用项显示 `—`。
- 数据预览只展示原始数据表，最多读取前 150 行，每页 20 行，展示字段表头并支持分页和横向滚动。
- 两个页签分别分页，表格支持固定首列。
- 鼠标进入弹窗后保持显示，并提供“固定”按钮；固定后点击页面空白处关闭。
- 键盘聚焦数据集名称时也可以打开弹窗。
- 明确标注“统计指标基于完整数据集，表格仅预览前 150 行”。

### 6.4 项目详情

- 顶部显示项目名称、任务类型、创建时间和项目状态。
- 数据集列表展示名称、上传时间、行列数、目标列、模型实验数和流程状态。
- 每个数据集提供“进入数据集”和“模型实验（数量）”两个按钮，卡片主体不承担隐含跳转。
- 提供“上传数据集”和“查看本项目模型”按钮。
- 尚未上传数据时允许修改任务类型；上传后该操作隐藏。

## 7. 数据集模块

### 7.1 数据集创建

- 点击“上传数据集”后打开统一弹窗，提供“上传 CSV”和“使用示例数据”两个入口。
- 示例数据只展示符合当前项目任务类型的数据集。
- 平台内置至少一套二分类示例和一套回归示例，并增加足够多的特征列用于测试类型检查、特征筛选和相关性功能。
- 默认使用 CSV 文件名作为数据集名称，允许修改。
- 同一项目内名称不得重复。
- 自动记录上传时间、行数和列数。
- 预览最多读取前 150 行，不代表完整数据量。
- 读取字段后由用户选择目标列；二分类同时选择正类。
- 创建项目时不选择目标列，项目允许在没有数据集的情况下直接创建。

### 7.2 CSV 检查

- 文件必须包含表头和至少一行数据。
- 表头不能为空、不能重复。
- 行列数不一致时阻止继续，并显示错误行号。
- 每列检查数值、日期、布尔、类别和文本类型的一致性。
- 类型混合时展示异常数量、异常示例和修正入口。

### 7.3 字段类型检查

字段类型检查放在数据集模块，表格字段包括：

| 字段 | 说明 |
| --- | --- |
| 字段名称 | CSV 表头名称 |
| 推断类型 | 数值、类别、日期、布尔或文本 |
| 用户确认类型 | 可手动修正 |
| 非空 / 缺失 | 数据完整性概览 |
| 唯一值数量 | 辅助识别类别、ID 和自由文本 |
| 一致性 | 一致或存在异常 |
| 训练可用性 | 可用于训练或不可用于训练 |
| 原因 | 日期、自由文本、疑似 ID 或类型异常等原因 |

### 7.4 不可训练字段

- 类别型字符串可以用于训练，并在特征工程中编码。
- 自由文本、疑似 ID 和原始日期字段标注“不可用于训练”。
- 不使用“建议排除”措辞。
- 数据集模块负责识别和解释，不提供排除开关。
- 目标列不受该规则限制。

### 7.5 目标列设置

- 用户完成字段检查后选择目标列。
- 二分类目标列必须具有两个有效类别，并选择正类。
- 回归目标列必须为数值类型。
- 不符合项目任务类型时阻止进入特征工程，并给出修正建议。

## 8. 特征准备与模型预处理

### 8.1 特征准备

“特征准备”页面只包含训练集 / 验证集划分和特征管理，并由同一数据集下所有模型实验共享。从数据检查进入时始终先显示划分页面，并保留用户已选择的比例。

页面内部顺序固定为：

1. 选择 70%/30%、75%/25%、80%/20% 或 90%/10%，默认 80%/20%。
2. 平台内部使用固定划分规则完成训练集 / 验证集划分，前端不展示随机种子。
3. 只使用训练集计算特征筛选指标。
4. 将最终特征集合应用到验证集。

二分类默认使用分层划分，保证训练集和验证集的类别比例接近。数据检查页展示类别分布和正类占比；类别明显不平衡时提示准确率不能单独用于判断模型效果。

### 8.2 特征筛选指标

| 任务 | 展示指标 |
| --- | --- |
| 二分类 | 缺失率、IV、PSI、特征间相关性 |
| 回归 | 缺失率、低方差、目标相关性、PSI、特征间相关性 |

- 回归任务不展示 IV，也不应用 `IV ≥ 0.01` 规则。
- 二分类默认缺失率阈值 90%、IV 阈值 0.01、PSI 上限 0.25、相关系数绝对值阈值 0.8，四项阈值均可调整并参与自动筛选或预警。
- “自动筛选”位于特征管理卡片右上角，始终使用系统内置推荐规则并直接更新纳入开关。
- 筛选栏提供“推荐阈值”和“确定修改”：前者只恢复推荐数字，后者应用用户当前填写的阈值并更新纳入开关。
- 数值特征相关性使用热力图展示，在 `|r| ≥ 0.8` 时预警，默认不自动排除。
- 相关性阈值位于热力图区块，修改后点击“确定修改”才更新预警；仅校验 `0–1`，不设置 `0.5` 业务下限。
- 提供“自动处理高相关特征”按钮，按当前数据动态生成保留字段、排除字段、相关系数和推荐理由，再由用户确认执行。
- 被自动排除的特征允许手动重新纳入，并继续显示高相关性提醒。
- IV、PSI、低方差和目标相关性提供悬浮解释、数值方向和参考范围；缺失率和特征间相关性不增加解释区间。
- 筛选栏下方提供“查看指标说明”折叠区。
- 所有指标标注“根据训练集计算”。
- 不可训练字段默认排除且开关禁用。
- 至少保留一个训练特征。
- 修改划分或特征集合后，已有成功实验保留为历史结果，不参与新实验的默认模型比较。

### 8.3 模型缺失值处理

缺失值处理属于模型实验配置。

| 字段类型 | 可选方式 |
| --- | --- |
| 数值 | 不处理、均值、中位数、众数、固定值、删除所在行 |
| 类别 | 不处理、保留为缺失类别、众数、固定值、删除所在行 |

| 模型 | 数值默认 | 类别默认 |
| --- | --- | --- |
| 逻辑回归、线性回归 | 均值 | 保留为缺失类别 |
| KNN | 中位数 | 保留为缺失类别 |
| 决策树、随机森林 | 中位数 | 保留为缺失类别 |
| LGBM | 不处理 | 不处理 |

用户可以逐字段修改或批量应用。可能导致训练失败的“不处理”设置显示警告，但不强制阻止选择。

### 8.4 标准化

- 逻辑回归、KNN 和线性回归默认开启。
- 决策树、随机森林和 LGBM 默认关闭。
- 用户可以覆盖默认值；不推荐配置只提示原因。
- 标准化属于模型实验配置。

### 8.5 防止数据泄漏

- 均值、中位数、众数、标准化均值和标准差只根据训练集计算。
- 使用训练集得到的预处理参数转换验证集。
- 删除缺失行时训练集和验证集分别处理。
- 不使用交叉验证；每组参数在训练集训练，并分别计算训练集与验证集指标。
- 验证集指标参与排序和 Top 3 选择，默认按验证集指标排序。
- 模型报告注明“所有预处理参数仅由训练集计算”。

## 9. 模型实验

### 9.1 创建实验

- 点击“新建模型实验”打开模型选择页。
- 每次只创建一个模型实验，不在一个流程中并行放置多个模型。
- 默认名称为“模型名称 + 两位序号”，允许修改。
- 同一数据集内实验名称不得重复。

### 9.2 模型范围

| 二分类 | 回归 |
| --- | --- |
| 逻辑回归 | 线性回归 |
| 决策树分类 | 决策树回归 |
| 随机森林分类 | 随机森林回归 |
| KNN 分类 | LGBM 回归 |
| LGBM 分类 |  |

暂不增加 SVM、XGBoost、神经网络和时序模型。

### 9.3 实验状态

- 草稿
- 可以训练
- 训练中
- 已完成
- 已取消
- 训练失败
- 历史结果

实验卡片展示实验名称、模型类型、标准化状态、调参方式、核心指标、更新时间和状态。

### 9.4 实验生命周期

- 草稿、训练失败和已取消实验允许修改并重新训练。
- 训练成功后配置与结果锁定，不能再次训练。
- 已完成实验在实验列表中同时提供“打开实验”和“进入训练结果”；打开实验后，底部操作栏仍提供“进入训练结果”。
- 需要调整时使用“复制为新模型实验”，名称自动递增。
- 项目、数据集和模型实验均允许删除；删除前二次确认，并级联清理下级对象、保存结果和 API 配置。
- 上游数据划分或特征配置变化后，成功实验保留为历史结果。

## 10. 参数与调参

### 10.1 基础参数

| 模型 | 默认展示参数 |
| --- | --- |
| 逻辑回归 | 正则化强度 `C` |
| 决策树 | 最大深度、最小叶节点样本数 |
| 随机森林 | 树数量、最大深度 |
| KNN | 邻居数量 |
| 线性回归 | 不展示可调参数 |
| LGBM | 树数量、学习率、叶子数 |

默认参数旁提供简短解释和推荐默认值。“高级参数设置”默认折叠，按模型展示真实、适用的可调参数，并提供解释、推荐默认值、范围校验和恢复默认值。枚举参数使用下拉框，例如划分标准、求解器、最大特征数、距离权重和距离度量；下拉框只展示当前模型支持的选项。连续或整数参数使用输入框。参数较少的模型可以不展示高级设置。前端完全不展示随机种子。训练结果记录实际使用的全部参数。

### 10.2 调参方式

可用方式根据模型适用性动态展示：

| 模型 | 快速自动调参 | 网格调参 | 贝叶斯调参 |
| --- | --- | --- | --- |
| LGBM 分类 / 回归 | 支持 | 支持 | 支持 |
| 随机森林分类 / 回归 | 支持 | 支持 | 支持 |
| 决策树分类 / 回归 | 支持 | 支持 | 不展示 |
| KNN 分类 | 支持 | 支持 | 不展示 |
| 逻辑回归 | 支持 | 支持 | 不展示 |
| 普通线性回归 | 不展示 | 不展示 | 不展示 |

不适用的选项直接隐藏，不展示禁用控件。

### 10.3 快速自动调参

- 作为推荐选项，用户无需配置算法或尝试次数。
- 只保留效果最好的三组训练结果。
- 不展示 Optuna、TPE 等技术名称。
- 只展示模拟阶段、进度和结果。
- 页面明确标注“模拟调参”。

### 10.4 网格调参

- 默认使用平台预设候选值，可以直接训练。
- 基础区域只展示最重要的 2 至 3 个调参参数，允许直接增删候选值。
- 用户可展开“更多调参参数”，通过复选框加入其他适用于当前模型的参数；新增参数自动带出可修改的推荐候选值。
- 不展示随机种子或不适用于当前模型的参数。
- 校验空列表、非数字、重复值、允许范围和组合数量。
- 实时显示参数组合数量，并提供“恢复预设值”。
- 组合数为各已启用参数候选值数量的乘积；候选值变化或启用更多参数后实时重新计算。
- 候选值无效时停止计算并提示具体参数；组合数较大时只显示风险提示，不设置强制上限。
- 不允许填写代码或复杂表达式。

### 10.5 贝叶斯调参

- 仅在 LGBM 和随机森林中展示。
- 当前阶段只模拟搜索过程，不执行真实优化。
- 不向初学者暴露算法细节。

## 11. 模拟训练

### 11.1 前置校验

开始训练前检查：

- 项目任务类型有效。
- 数据集、目标列和正类有效。
- 字段检查和特征工程已完成。
- 至少存在一个可训练特征。
- 模型类型与项目任务一致。
- 参数和调参候选值有效。

### 11.2 训练状态

- 校验配置
- 准备数据
- 应用特征工程
- 训练模型或快速自动调参
- 计算指标
- 生成结果

总时长控制在 5–8 秒。用户可以取消；取消后不生成新的模型结果。

训练进度必须使用居中弹窗展示当前阶段、进度条和百分比，不允许退化为页面底部进度条。弹窗不能通过遮罩关闭，支持取消和最小化；训练完成后自动关闭并进入训练结果，失败时展示原因和返回修改入口。

### 11.3 Mock 原则

- 相同项目、数据集、特征、模型和参数产生稳定结果。
- 所有模拟结果显著标注“模拟结果”或“模拟调参”。
- Mock Service 与 UI 分离，未来可替换为真实 API。

## 12. 训练结果

### 12.1 结果层级

- 使用表格展示，不使用卡片。
- 默认展示综合效果最好的 Top 3，“查看全部结果”展开完整表格。
- 顶部展示项目、数据集、模型实验和模型类型。
- 页面顶部提供数据范围和排序指标下拉框，数据范围可选训练集或验证集，默认验证集。
- 每行提供选择框和“查看模型报告”；查看按钮直接打开该方案报告，不再区分主方案。

### 12.2 分类结果指标

- 训练集和验证集均展示 AUC、KS、F1、准确率、精确率和召回率，默认按验证集 AUC 降序。
- AUC、KS、F1、准确率、精确率和召回率均降序；训练 / 验证差值升序。

### 12.3 回归结果指标

- 训练集和验证集均展示 RMSE、MAE 和 R²，默认按验证集 RMSE 升序。
- RMSE、MAE 和训练 / 验证差值升序；R²降序。
- MAE 和 RMSE 不设置通用固定阈值，只与基线或其他实验比较。

### 12.4 指标交互

- 指标表头提供信息图标、简短解释和“越大越好 / 越小越好”。
- 切换数据范围或排序指标后重新计算排名和 Top 3。
- 默认只勾选当前排序指标下排名第 1 的结果，允许多选，至少保存一组。
- 用户尚未手动选择时，切换排序指标自动勾选新排序下第 1 名；用户已手动选择后保留其选择。
- 页面提供“保存所选结果到模型库”按钮，保存后跳转模型库；已保存结果显示“已保存”，避免重复保存。
- 保存时同步写入模型库已保存方案集合，并重置可能隐藏该结果的模型库筛选条件，确保跳转后立即可见。
- 如果所选结果均已保存，则不重复创建记录，并提示“所选结果已保存到模型库”。
- Top 3 是同一模型实验下的参数方案，不是三个独立模型。

## 13. 模型报告

模型报告不属于流程步骤，是训练结果的下级详情页。报告页不显示步骤条，底部操作栏左侧提供“返回训练结果”。

### 13.1 二分类报告

- 核心指标：AUC、KS、F1、准确率、精确率和召回率。
- 同时展示训练集指标、验证集指标和两者差值。
- F1 标题旁提供统一信息图标，悬浮说明“精确率与召回率的综合指标，越大越好。”；指标卡片显示“适合同时关注误报和漏报的场景。”
- ROC 曲线、KS 曲线、混淆矩阵和 Gain Table。
- 默认展示验证集混淆矩阵，不额外展示训练集混淆矩阵；矩阵标注当前分类阈值。
- 混淆矩阵展示 TP、TN、FP、FN，并基于矩阵计算准确率、精确率、召回率和 F1；报告展示简短计算公式。
- 指标分母为 0 时显示 `—` 和“当前数据无法计算”，不显示 `NaN` 或无穷值。
- Gain Table 至少展示累计人群比例、累计正类捕获率和 Lift。
- Lift 解释为：“模型筛选效果相对随机筛选提升了多少倍，越大越好。”
- 示例：“Lift = 3，表示命中率约为随机筛选的 3 倍。”

### 13.2 回归报告

- 核心指标：MAE、RMSE 和 R²。
- 同时展示训练集指标、验证集指标和两者差值。
- 真实值与预测值对比图和残差分布。
- 指标与其他实验或基线的对比提示。

### 13.3 数据概览

- 只统计最终参与训练的特征。
- 数值特征展示样本数、缺失率、均值、中位数、标准差、最小值和最大值。
- 类别特征展示样本数、缺失率和唯一值数量。
- 类别特征不展示众数。

### 13.4 变量重要性

最多展示 Top 10，特征不足时展示全部。已排除和不可训练字段不得出现。

| 模型 | 计算方式 | 页面标注 |
| --- | --- | --- |
| 决策树 | 节点分裂带来的不纯度下降 | 树模型原生重要性：不纯度下降 |
| 随机森林 | 各树不纯度下降重要性的平均值 | 树模型原生重要性：平均不纯度下降 |
| LGBM | 累计分裂增益 | 树模型原生重要性：分裂增益 Gain |
| 逻辑回归、线性回归 | 标准化特征后的模型系数 | 标准化系数，保留正负方向 |
| KNN | 排列重要性 | Permutation Importance |

不同计算方式的分数不可跨模型直接比较。

### 13.5 指标说明

- 指标名称旁提供信息图标和简短说明。
- 报告增加“指标说明”折叠区，展示完整含义、参考区间和优化方向。
- 分类指标、回归指标、二分类 IV 和两类任务的 PSI 分别使用对应说明。
- 缺失率和相关系数不增加解释区间。
- 模型报告展示训练集和验证集表现，并注明所有预处理参数仅由训练集计算。

## 14. 模型库与 API 接入配置

### 14.1 模型比较

- 模型库按“项目 → 数据集 → 模型实验 → 已保存参数方案”分层。
- 项目筛选提供“全部项目”选项；从全局入口进入时默认展示全部项目，从项目或数据集进入时自动带入对应筛选条件。
- 数据集筛选随项目筛选联动；默认展示筛选范围内所有模型类型。
- 支持按模型类型筛选；同一模型类型的多个实验分别显示。
- 模型类型筛选框默认选择全部；选择单一类型后，集中比较该类型下各实验的已保存结果。
- 每个已保存参数方案作为独立结果行展示，并标明所属模型实验。
- 同一次实验保存多组参数结果时，每组结果形成独立模型库记录，但不创建新的模型实验。
- 保存动作先写入模型库数据再跳转；新增记录立即展示，并包含项目、数据集、实验、模型类型、参数摘要、训练集指标、验证集指标、创建时间和模型报告入口。
- 排序指标和方向与训练结果页面一致。
- 新保存结果立即参与项目、数据集、模型类型筛选和指标排序。
- 历史、需要重新训练和失败结果保留展示，但不参与默认排序。
- 每个方案可返回来源实验查看完整训练结果。
- 页面标注“模型指标包含数据预处理方式带来的影响”。

### 14.2 API 接入配置

模型库包含“模型比较”和“API 服务”两个页签。API 不属于训练主流程，只放在模型库的更多操作中。

- 功能名称使用“生成 API 接入配置”，不使用“部署为 API”。
- 仅有效、已完成、已保存的参数方案可以生成。
- 生成时可选择任一已保存方案。
- API 配置绑定具体参数方案。
- 明确标注“模拟 API 接入配置，不可用于真实业务调用”。

API 页面展示：

- API 服务名称、来源项目、数据集、模型实验和参数方案。
- 模拟接口地址和请求方法。
- 输入字段与 JSON 请求示例。
- JavaScript、Python 和 cURL 调用示例及复制按钮。
- 返回字段说明和模拟响应。
- “测试请求”按钮，返回稳定模拟预测结果。
- 更新模型方案和删除 API 配置。

不实现启用 / 停用、API 密钥、权限、调用统计、额度和版本回滚。

## 15. 状态与数据模型

```ts
type TaskType = 'binary-classification' | 'regression';

interface Project {
  id: string;
  name: string;
  taskType: TaskType;
  createdAt: string;
  updatedAt: string;
  datasetIds: string[];
}

interface Dataset {
  id: string;
  projectId: string;
  name: string;
  uploadedAt: string;
  rowCount: number;
  columnCount: number;
  targetColumn?: string;
  positiveClass?: string;
  previewRows: Record<string, unknown>[];
  columns: ColumnProfile[];
  featureEngineering: SharedFeatureConfig;
  status: 'unchecked' | 'configuring' | 'ready';
}

interface ColumnProfile {
  name: string;
  inferredType: 'number' | 'category' | 'date' | 'boolean' | 'text';
  selectedType: 'number' | 'category' | 'date' | 'boolean' | 'text';
  missingCount: number;
  uniqueCount: number;
  consistent: boolean;
  trainable: boolean;
  unavailableReason?: string;
  included: boolean;
  iv?: number;
  psi?: number;
}

interface SharedFeatureConfig {
  selectedFeatures: string[];
  thresholds: { missing: number; iv: number; psi: number; correlation: number };
  splitPreset: '70-30' | '75-25' | '80-20' | '90-10';
  revision: number;
}

interface ModelExperiment {
  id: string;
  projectId: string;
  datasetId: string;
  name: string;
  modelType: string;
  missingStrategy: Record<string, string>;
  standardize: boolean;
  tuningMethod: 'none' | 'auto' | 'grid' | 'bayesian';
  parameters: Record<string, unknown>;
  status: 'draft' | 'ready' | 'training' | 'completed' | 'cancelled' | 'failed' | 'historical' | 'archived';
  trainedFeatureRevision?: number;
  resultIds: string[];
  selectedResultId?: string;
  savedResultIds: string[];
  createdAt: string;
  updatedAt: string;
}

interface ParameterResult {
  id: string;
  experimentId: string;
  parameters: Record<string, unknown>;
  metrics: Record<string, number>;
  rank: number;
  savedToLibrary: boolean;
  createdAt: string;
}
```

LocalStorage 只保存必要的结构化状态。CSV 完整内容不长期保存，当前仅保存最多 150 行预览和字段统计。

## 16. 前端技术方案

### 16.1 推荐技术栈

- React 18 + TypeScript
- Vite
- React Router
- Zustand
- React Hook Form + Zod
- TanStack Table
- ECharts
- Vitest + React Testing Library
- Playwright

### 16.2 模块边界

```text
src/
├─ app/                 # 路由、全局布局、错误边界
├─ pages/
│  ├─ home/
│  ├─ projects/
│  ├─ datasets/
│  ├─ feature-engineering/
│  ├─ experiments/
│  ├─ tuning-results/
│  ├─ reports/
│  ├─ model-library/
│  └─ api-demo/
├─ components/
│  ├─ dataset-hover/
│  ├─ stepper/
│  ├─ data-table/
│  ├─ metric-help/
│  └─ charts/
├─ stores/
├─ services/
│  ├─ csv.service.ts
│  ├─ profile.service.ts
│  ├─ mock-training.service.ts
│  └─ mock-api.service.ts
├─ domain/
└─ utils/
```

### 16.3 服务层约束

- UI 不直接生成训练指标。
- 稳定 Mock 逻辑集中在 Service 层。
- 所有 Mock 请求保持异步接口形式。
- 未来真实后端沿用相同请求和响应模型替换实现。

## 17. 开发阶段

### 阶段一：项目与数据集

- 重构全局状态为项目制层级。
- 实现项目创建、数据集上传 / 示例入口和数据集悬浮弹窗。
- 实现多数据集、目标列和前 150 行预览。
- 完成字段类型检查与“不可用于训练”标记。

**验收：** 可以创建分类或回归项目，管理多个数据集并完成数据检查。

### 阶段二：特征准备与预处理

- 移除画布、拖拽、节点、连线和缩放代码。
- 实现固定步骤条和状态流转。
- 实现训练集 / 验证集划分、分类 / 回归差异化特征筛选和训练集指标计算。
- 实现模型级缺失值处理、标准化和防数据泄漏规则。

**验收：** 用户完成特征准备，所有筛选和预处理参数仅由训练集计算。

### 阶段三：模型实验与调参

- 实现任务类型过滤后的模型选择。
- 实现实验创建、复制、锁定、精简参数和高级参数配置。
- 增加快速自动调参并保留适用的网格、贝叶斯调参。
- 实现训练进度弹窗、最小化和取消。

**验收：** 同一数据集可创建多个模型实验，并完成普通训练或模拟调参。

### 阶段四：结果、模型库与 API

- 实现表格化 Top 3、全部结果、排序下拉和方案保存。
- 完善分类与回归指标、解释、排序方向和报告。
- 实现模型比较、实验方案分组和来源实验跳转。
- 实现模型库内模拟 API 接入配置和测试请求。

**验收：** 用户能理解指标、选择最佳方案、查看模型报告并体验模拟 API。

### 阶段五：验证与收尾

- 补充空状态、错误状态、加载状态和失效提示。
- 验证 LocalStorage 恢复、键盘操作和桌面端布局。
- 删除旧画布相关死代码和样式。

## 18. 测试与验收

### 18.1 核心测试

| 类型 | 范围 |
| --- | --- |
| 单元测试 | CSV 解析、字段类型、不可训练规则、筛选阈值、配置失效、训练结果排序 |
| 组件测试 | 数据集弹窗、数据预览、步骤条、特征筛选、模型选择、指标帮助 |
| 集成测试 | 项目到模型报告的分类与回归完整流程 |
| 端到端测试 | 创建项目、上传数据、特征工程、多实验、Top 3、模型库、模拟 API |

### 18.2 关键验收标准

- 项目任务类型在上传数据后不可修改。
- 一个项目可管理多个数据集，一个数据集可创建多个模型实验。
- 项目不显示弹窗；数据集悬浮弹窗可固定、切换分页并预览前 150 行数据。
- 字段类型检查位于数据集模块。
- 自由文本、疑似 ID 和日期字段标注“不可用于训练”。
- 页面不再出现自由画布、拖拽、连线、缩放和平移功能。
- 固定流程使用“数据检查 → 特征准备 → 模型训练 → 训练结果”，模型报告作为结果详情页。
- 回归任务不展示 IV；二分类默认分层划分。
- 特征筛选、填充值和标准化参数只根据训练集计算。
- 平台使用训练集和验证集，不使用测试集与交叉验证；预处理参数只根据训练集计算。
- 分类项目不展示回归模型，回归项目不展示分类模型。
- 快速自动调参不展示算法名称和尝试次数。
- 训练结果使用表格，默认展示 Top 3，可按训练集或验证集指标排序并查看对应模型报告。
- 分类、回归指标使用正确的排序方向和说明。
- 缺失率和相关系数不展示额外参考区间。
- 变量重要性明确标注计算方式。
- 成功实验锁定；调整时复制为新实验，失败或取消实验允许重新训练。
- 模型库作为全局一级模块，项目筛选提供“全部项目”，并按项目、数据集、实验和已保存方案分层。
- API 接入配置只在模型库中出现，并绑定具体已保存方案。
- 刷新页面后项目、数据集、实验和结果状态可以恢复。
