# 机器学习平台前端修改需求 V5

本文档记录基于《机器学习平台待修改需求 v5》逐项确认后的前端修改内容。未列出的功能保持 V4 设计，不增加额外功能。

## 1. 特征筛选阈值与相关性

- 特征管理界面恢复筛选阈值设置。
- 缺失率上限默认 `90%`。
- 二分类任务的 IV 下限默认 `0.01`。
- PSI 上限默认 `0.25`，纳入自动筛选。
- 回归任务展示低方差阈值和目标相关性下限。
- 用户修改阈值后，点击“应用筛选”才更新自动纳入结果。
- 不可用于训练的字段始终不能被自动纳入。
- 增加数值特征相关性热力图，并提供可调整的相关性筛选阈值，默认在 `|r| ≥ 0.8` 时预警。
- 高相关性默认只预警，不自动排除。
- 提供“自动处理高相关特征”按钮，点击后先展示调整预览，再由用户确认执行。
- 每组高相关特征优先保留缺失率更低、IV 或目标相关性更高的特征；指标相同时，优先保留唯一值和类型稳定性更合理的特征。
- 被自动排除的特征允许手动重新纳入，并继续标注高相关性提醒。
- 筛选指标和相关性矩阵只使用训练集计算。

## 2. 模型高级参数

- 基础区域只展示每个模型最重要的 1 至 3 个参数。
- 高级参数按模型展示真实、适用的可调参数。
- 每个参数提供简短解释、推荐默认值和合理输入范围。
- 高级参数默认折叠。
- 参数较少的模型可以不展示高级参数入口。
- 前端完全不展示随机种子。

## 3. 网格调参

- 选择网格调参后，默认展示一组推荐候选值。
- 基础区域只展示最重要的 2 至 3 个调参参数，并允许直接增删候选值。
- “更多调参参数”默认折叠。
- 用户通过复选框选择需要加入网格的其他参数。
- 新加入的参数自动带出推荐候选值，并允许修改。
- 不展示随机种子或不适用于当前模型的参数。
- 保留“恢复推荐值”操作。

## 4. F1 指标解释

- F1 标题旁增加悬浮说明图标。
- 悬浮文案为：“精确率与召回率的综合指标，越大越好。”
- F1 指标卡片增加简短说明：“适合同时关注误报和漏报的场景。”
- 报告底部继续保留完整指标解释。
- AUC、KS、F1 使用统一的悬浮提示样式。

## 5. 调参结果保存

- 默认只展示当前排序指标下的 Top 3 结果。
- 用户点击“查看全部结果”后展开全部结果。
- 默认只勾选当前排序指标下排名第 1 的结果。
- 用户可以勾选其他结果，支持多选。
- 不再区分或标注主方案，删除“设为主方案”操作。
- 页面提供“保存所选结果到模型库”按钮。
- 未勾选结果时禁用保存，并提示至少选择一个结果。
- 保存完成后跳转到对应项目和数据集的模型库。
- 已保存结果显示“已保存”，避免重复保存。
- 用户尚未手动选择时，切换排序指标会默认勾选新排序下第 1 名；用户已经手动选择后，切换排序保留其选择。
- 模型库将每个已保存参数方案作为独立结果行展示，并标明其所属模型实验。

## 6. 范围约束

- 本轮只实现以上五项前端变化。
- 不增加后端训练、真实模型服务、权限、监控、版本发布或其他未确认功能。

## 7. 模型库模型类型筛选

- 模型库增加模型类型筛选框，默认展示全部模型。
- 选择某一模型类型后，只展示该类型下各模型实验已保存的结果，便于横向比较同一模型。

## 8. 数据集悬浮弹窗

- 数据概览展示前 50 个特征的统计指标，并支持分页。
- 数据预览展示字段表头，并支持分页。
- 数据预览仍最多读取前 150 行。

## 9. 删除操作

- 每个项目、数据集和模型实验都提供删除操作。
- 删除前必须二次确认，并明确提示关联数据会同时删除。
- 删除项目时同步删除其数据集、模型实验和 API 配置。
- 删除数据集时同步删除其模型实验和 API 配置。
- 删除模型实验时同步删除其调参结果、模型库保存结果和 API 配置。

## 10. 相关性阈值与快速自动调参

- 相关性预警阈值从自动筛选阈值区域移动到相关性热力图区块。
- 选择快速自动调参时，只保留效果最好的三组调参结果。
