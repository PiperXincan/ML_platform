# 机器学习平台前端修改需求 V3

## 1. 文档目的

本文档记录《机器学习平台待修改需求v3.docx》及后续逐项讨论确认的结果，作为更新前端开发文档和升级网页的直接依据。

当前阶段仍只实现桌面 Web 前端。数据分析、特征工程、调参、训练、模型报告和 API 调用均使用稳定 Mock 数据，不接入真实机器学习服务。

## 2. 产品定位

- 面向了解少量机器学习知识、希望通过零代码完成训练的初学者。
- 当前只支持二分类和普通回归。
- 暂不支持多分类、时序预测、聚类、深度学习和真实模型部署。
- 时序预测未来作为独立任务类型建设；当前日期字段不可直接用于训练。

## 3. 项目制结构

平台采用以下层级：

`项目 → 数据集 → 模型实验 → 参数结果`

### 3.1 项目

- 创建项目时只填写项目名称和任务类型，系统自动记录创建时间。
- 任务类型只能选择二分类或回归。
- 项目尚未上传数据时可以修改任务类型。
- 上传数据或产生训练记录后，任务类型不可修改。
- 一个项目可以包含多个数据集。

### 3.2 数据集

- 一个数据集可以创建多个模型实验。
- 默认使用 CSV 文件名作为数据集名称，允许修改显示名称。
- 同一项目内数据集名称不能重复。
- 数据集记录上传时间、行数、列数和目标列。
- 每个数据集只设置一个目标列，同一数据集下所有模型实验共享该目标列。
- 开始特征工程或训练后修改目标列，将清空特征配置并使已有训练结果失效。
- 如需预测另一个目标，用户应复制或重新上传为新数据集。

### 3.3 模型实验

- 一个模型对应一个模型实验。
- 同一模型类型可以创建多个实验。
- 默认名称为“模型名称 + 序号”，允许修改。
- 同一数据集内实验名称不能重复。
- 实验卡片始终展示模型类型、实验状态和核心指标。

## 4. 导航与项目悬浮弹窗

- 侧边栏增加项目入口和项目列表。
- 点击项目进入项目详情页。
- 鼠标停留项目名称约 300ms 后打开悬浮弹窗。
- 鼠标移出后延迟关闭，用户可以将鼠标移入弹窗继续操作。
- 弹窗包含“项目概览、数据集指标、数据预览”三个分页。
- 数据预览最多读取前 150 行，每页 20 行。
- 弹窗设置固定最大宽高，表格区域内部滚动。
- 明确提示“当前仅预览前 150 行”。

## 5. 数据集模块

### 5.1 数据上传

- 仅支持 CSV。
- 上传后展示数据集概览、目标列、字段类型检查和前 150 行预览。
- 字段类型包括数值、类别、日期、布尔和文本。

### 5.2 字段类型检查

- 字段类型检查属于数据集模块，不属于特征工程模块。
- 展示推断类型、非空数、缺失数、唯一值数、一致性和异常示例。
- 用户可以修正推断类型。
- 数据集模块只识别字段风险，不在此处执行特征排除操作。

### 5.3 不可训练字段

- 类别型字符串允许进入训练，并在特征工程中进行编码。
- 自由文本、疑似 ID 和原始日期字段统一标注“不可用于训练”。
- 展示不可训练的具体原因。
- 目标列不受特征排除规则限制。
- 不使用“建议排除”等弱提示措辞。

## 6. 特征工程模块

特征工程独立于数据集模块和模型实验，采用固定顺序：

1. 缺失值处理
2. 特征筛选
3. 标准化
4. 训练集划分

- 用户可以跳过非必要步骤，但不能调整步骤顺序。
- 同一数据集下的模型实验共享缺失值处理、特征筛选和训练集划分配置。
- 修改共享配置后，已有模型实验标记为“需要重新训练”。
- 不可用于训练的字段仍展示在特征筛选列表，但默认排除且开关禁用。

### 6.1 特征筛选

- 汇总字段类型、缺失率、IV、PSI 和特征间相关性。
- 默认缺失率阈值为 90%，IV 阈值为 0.01，PSI 阈值为 0.25，相关系数绝对值阈值为 0.8。
- 高相关特征根据 IV 较高优先、缺失率较低次优的规则推荐保留项。
- 用户可以采用推荐，也可以手动排除和恢复普通可训练字段。
- 至少保留一个训练特征。

### 6.2 标准化

- 标准化属于模型实验的例外配置，而不是完全共享配置。
- 逻辑回归、KNN 和线性回归默认开启。
- 决策树、随机森林和 LGBM 默认关闭。
- 用户可以手动覆盖；不推荐的设置只提示原因，不阻止训练。

### 6.3 训练集划分

- 提供 70%/30%、75%/25%、80%/20% 和 90%/10% 四种预设比例。
- 默认使用 80%/20%。

## 7. 固定步骤式训练流程

- 彻底取消自由画布、节点拖拽、连线、缩放和平移。
- 固定流程为：`数据检查 → 特征工程 → 模型实验 → 调参结果 → 模型报告`。
- 顶部步骤条显示未开始、进行中、已完成和需要重新训练状态。
- 用户可以返回修改前序步骤。
- 修改目标列、字段类型或共享特征工程后，保留旧结果供查看，但标注“需要重新训练”。

## 8. 模型范围

### 8.1 二分类

- 逻辑回归
- 决策树分类
- 随机森林分类
- KNN 分类
- LGBM 分类

### 8.2 回归

- 线性回归
- 决策树回归
- 随机森林回归
- LGBM 回归

- 分类项目只展示分类模型，回归项目只展示回归模型。
- 暂不增加 SVM、XGBoost 和神经网络。

## 9. 调参

支持的训练方式按模型适用性展示：

- 不调参
- 快速自动调参（推荐）
- 网格调参
- 贝叶斯调参

### 9.1 快速自动调参

- 用户无需配置搜索算法或尝试次数。
- 前台不展示 Optuna、TPE 等技术名称。
- 只展示当前阶段、进度和模拟结果。
- 纯前端阶段明确标注“模拟调参”。

### 9.2 手动调参方式

- 网格调参默认使用预设候选值，可展开高级设置修改、校验和恢复。
- 贝叶斯调参仅在适合的模型中展示。
- 不适合网格或贝叶斯调参的模型不展示对应选项。
- 普通线性回归不展示调参区域。

## 10. 调参结果

- 每次训练保存全部模拟调参结果。
- 默认只展示综合效果最好的 Top 3。
- 点击“查看全部结果”进入完整列表，点击单条结果进入详情。
- 顶部展示所属项目、数据集、模型实验和模型类型。
- 分类默认按验证集 AUC 排序，并展示 KS、F1、准确率、召回率和过拟合差值。
- 回归默认按验证集 RMSE 排序，并展示 MAE、R² 和过拟合差值。
- 用户可以切换主要排序指标。
- 每个指标标注“越大越好”或“越小越好”。

## 11. 模型报告与指标说明

- 指标名称旁提供信息图标，悬浮显示简短含义和优化方向。
- 报告增加“指标说明”折叠区，展示完整解释和参考区间。
- 分类指标包括 AUC、KS、F1、准确率、精确率和召回率。
- 回归指标包括 MAE、RMSE 和 R²。
- MAE、RMSE 不设置固定优劣阈值，只与基线或其他实验比较。
- IV、PSI 在特征筛选页提供解释和参考范围。
- 缺失率和相关系数只展示当前值及筛选阈值，不额外标注优劣方向或参考区间。
- 变量重要性必须标注计算方式；不同方法的分数不可跨模型直接比较。

## 12. 模型库与 API

- 模型库保留一个全局入口，并按项目、数据集和模型实验分组。
- 支持按项目、任务类型、模型类型和状态筛选。
- 每个模型实验只生成一张模型卡片，展示当前选中的最佳结果。
- Top 3 和全部历史结果保留在模型实验详情中。
- 切换最佳参数方案后，模型库卡片同步更新。
- 未完成、已取消或需要重新训练的实验不提供 API 入口。

纯前端阶段提供模拟 API 接入说明：

- 展示模拟接口地址、请求方法和 JSON 参数示例。
- 支持复制 JavaScript、Python 和 cURL 示例。
- “测试请求”只返回模拟预测结果。
- 明确标注“演示接口，尚未部署真实模型服务”。
- 不实现 API 密钥、调用统计、额度和服务启停。

## 13. 非必要功能限制

- 不实现登录、权限、协作、计费和云端存储。
- 不实现数据集版本管理、项目标签、实验标签、备注和收藏。
- 不实现真实训练、真实自动调参、真实模型 API 和部署。
- 不实现移动端和平板适配。
