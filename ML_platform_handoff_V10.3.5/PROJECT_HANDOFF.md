# ML Studio 前端项目交接说明

> 面向后续开发者或大模型。优先遵循本文件列出的最终结论，不要根据旧版本需求自行恢复已取消的设计。

## 1. 项目定位

- 目标：面向机器学习初学者的零代码桌面 Web 平台，仅实现前端演示。
- 用户通过固定步骤和点击操作完成数据检查、特征准备、模型训练与结果查看。
- 当前任务范围：二分类、多分类（3–20 类）、普通回归、单序列时序预测。
- 当前不做：真实模型训练、真实后端、真实部署、登录权限、云端存储。
- 技术形态：原生 HTML、CSS、JavaScript，使用 `LocalStorage` 保存演示状态，无构建步骤。
- 仓库：[PiperXincan/ML_platform](https://github.com/PiperXincan/ML_platform)
- 当前主分支：`main`
- V10.2.2 功能基线提交：`a083863 feat: refine time-series feature and tuning UX v10.2.2`
- V10.3.5 在 V10.3.4 基础上继续做智能选型 UI/交互优化；当前浏览器验证已通过，未执行 Node 语法/回归测试（宿主未安装 Node）。
- 精简 AI 交接文档提交：`b79b206 docs: add concise AI project handoff`

## 2. 事实来源优先级

发生冲突时按以下顺序判断：

1. 用户最新明确要求。
2. 本文件的 V10.3.5 与 V9.0.0 最终结论。
3. `机器学习训练平台前端开发文档.md`。
4. `frontend-change-requirements-v7.md` 及更早需求，仅用于追溯。

基本原则：只做需求明确要求的修改，不顺带重构或重新设计其他页面。

## 3. 当前入口与文件

| 文件 | 用途 |
| --- | --- |
| `index.html` | 当前页面入口，只加载 `app.css` 和 `app.js` |
| `app.js` | 当前完整 JavaScript；保留历史分段，最终由文件末尾 V10.3 覆盖层生效 |
| `app.css` | 当前完整样式；保留历史分段并追加 V10.3 样式 |
| `frontend-change-requirements-v7.md` | V7 历史需求，仅用于追溯 |
- 机器学习训练平台前端开发文档.md | 当前完整开发文档 V10.3.5
| `qa/smoke.test.js` | CSV、模拟结果、目标列、分页和 Hash 路由回归检查 |

当前采用单入口文件结构。原 V3–V8 的执行和层叠顺序已原样保留在 `app.js` 与 `app.css` 中；后续修改直接编辑这两个文件，不再新增 `v9.js`、`v9.css` 一类增量文件。需要追溯旧版本时使用 Git 历史和版本标签。

V9.0.1 统一了多分类与二分类的阈值输入体验：非法范围实时标红并显示行内错误；多分类恢复特征相关性热力图；非数值特征的归一化方差显示为“不适用”；训练结果页可正常进入所选方案的模型报告。

V9.1.0 增加标准 CSV 引号解析、20MB/UTF-8 检查、固定模拟结果来源、模型训练快照、分类和回归统一安全修改目标列、30 行数据预览分页、PDF 导出、Hash 路由、弹窗焦点管理、真实接口复制、API 删除确认、点击式表头筛选和 1024px 桌面适配；顶部不再展示“已本地保存”。V9.0.11 将模型库指标对比表的列宽策略与训练结果表统一。V9.0.10 的本地报告保存演示已由 V9.1.0 的浏览器 PDF 导出替代。V9.0.9 增加分类任务安全修改目标列。

V9.1.1 收口关键页面跳转：训练结果重复保存也必须进入对应模型库详情；模型库打开报告后返回模型库；所有同步状态式页面切换都更新 Hash；无训练结果时禁用训练结果和模型报告步骤。

V10.0.0 增加独立的单序列时序预测流程，并继续使用原生 JavaScript/CSS 和 `mlStudioV4`。所有预测、回测、预测区间与 API 响应均为稳定 Mock；同一训练结果在训练结果、报告、模型库和 API 页面保持一致。

V10.1.0 保持单实体、单序列和单目标边界，但允许多个外部特征参与预测。未来目标值可以留空，未来外部特征按覆盖率筛选并允许配置缺失处理。基线预测和指数平滑只使用目标历史；ARIMA 有外部特征时切换为 ARIMAX；Prophet 与 LightGBM 支持外部特征。

V10.1.1 修复未来空目标被当成 0 的问题，并收紧未来覆盖率和历史缺失值处理。模型库报告绑定保存时的不可变快照。时序 Mock API 采用完整方案：使用外部特征的 ARIMAX、Prophet 与 LightGBM 请求必须逐期提供未来特征；基线预测和指数平滑仍只需预测期数。

V10.2.0 取消上传文件中的未来数据设计：数据集只含历史观测，未来外部特征仅在 API 请求时提供。数据检查增加字段类型表和历史趋势图；时间列只能从日期型字段中选择，手动改为日期类型时必须整列解析通过。预测期数按频率上限与 3 轮回测可用历史共同限制。自动筛选只保留历史缺失率与归一化方差，相关性与最佳滞后仅作参考，热力图始终展开。

V10.2.1 阻止分类/回归结果增强器在 `bind()` 阶段二次改写时序训练结果，时序表只使用 MAE、RMSE、sMAPE、稳定性；训练结果与模型库的最右操作列由表格内部滚动容器负责固定吸附。

V10.2.2 修复日历特征开关的样式作用域并增加明确状态；特征阈值主动作改为“自动筛选”，完成后展示纳入与排除数量。时序模型实验页增加独立参数结构，避免被分类/回归增强器覆盖；常用参数始终展开，高级参数按模型折叠，自动调参展示推荐范围，网格调参支持候选值、组合数和上限校验。

V10.3.0 新增智能选型：在模型训练页首位插入“推荐入口”卡片；支持二分类、多分类、回归和时序任务。系统按任务规则判断模型可用性并给出预推荐模型池；用户可调整模型池（分类/多分类/时序最多 5 个、回归最多 4 个，时序基线强制参与）。以固定参数逐个运行候选模型，按任务专属主指标排序并推荐模型（效果接近时优先更简单模型）。结果页默认展示推荐模型，可展开完整对比表；推荐模型和其他成功模型均可一键创建草稿实验。历史记录区显示当前数据集最近 3 条记录，支持查看和删除；配置变化后旧记录标记失效。智能选型结果不进入模型库，不生成报告和 API。

V10.3.1 优化智能选型体验：平台基准配置折叠区以中文键值对展示固定参数；运行弹窗复用训练进度弹窗结构并补齐最小化卡片（支持最小化、展开、取消）；结果页“选择其他模型”改为跳转模型选择页，并新增“查看全部对比结果”按钮原地展开表格；模型选择页为推荐模型卡片注入绿色“★ 智能选型推荐”徽章；智能选型卡片在存在有效记录时显示“最近推荐：{模型名}”，并提供“查看选型结果”和“重新选型”入口。

V10.3.2 进一步优化智能选型体验细节：时序特征相关性热力图按钮区与分类/回归版对齐，删除「恢复 0.80」按钮，将预警阈值输入框、「尚未应用」提示和「确定修改」按钮统一包入 `.correlation-actions`；推荐模型卡片徽章改为右上角绝对定位并放大；智能选型创建实验的命名与普通实验完全一致（`{模型名} {两位序号}`），并在启动时一次性迁移旧数据中名称含“智能选型”的实验。

### 3.1 V9.0.0 分类任务最终结论

- 创建项目只选择“分类”或“回归”。分类项目在第一个有效数据集点击“下一步：特征准备”并确认时，根据目标列锁定为二分类或多分类。
- 项目分类子类型锁定后，数据集仍可更换符合相同子类型的目标列。没有模型实验时可原地修改并重置目标相关配置；只要已有实验、模型库快照或 API 配置，就只能复制数据集后修改。候选目标属于另一分类子类型时必须新建项目。
- 回归数据集采用相同保护规则；回归候选目标列必须是有效数值型字段。
- 锁定后同一项目只能添加相同分类子类型的数据集；任务不同提示新建项目。删除项目内全部数据集后解除锁定。
- 2 类为二分类，3–20 类为多分类，超过 20 类不支持。目标空值不填充并排除训练；空值超过 10% 警告。
- 划分后每类必须在训练集和验证集各保留至少 1 条样本；80/20 至少 5 条，90/10 至少 10 条。
- 二分类保留正类且自动预选，允许修改；多分类不设置正类。多分类 3–10 类用环形占比图，11–20 类用横向条形图。
- 多分类沿用逻辑回归、决策树、随机森林、KNN、LGBM 五个模型家族，不增加算法；逻辑回归按 Softmax 多项逻辑回归展示。
- 多分类默认指标为 Macro F1 和 Accuracy，默认按验证集 Macro F1 降序；可选 Weighted F1、Macro Precision、Macro Recall、Log Loss 和 Macro F1 差值。
- 多分类特征筛选不使用 IV 和互信息。使用缺失率、归一化方差、PSI、特征相关性；方差仅对训练集 Min-Max 归一化后的数值特征计算，默认阈值 0.01。
- 多分类报告展示指标概览、每类 Precision/Recall/F1/Support、Top 3 混淆方向、变量重要性和数据概览。3–9 类展示完整混淆矩阵，10 类及以上不展示。
- 模型库全局索引只展示任务标签和管理信息，不跨项目比较指标；项目/数据集/实验详情使用该项目唯一任务类型的指标。
- 从训练结果保存模型后直接打开所属模型实验的模型库详情，保留来源上下文并显示“返回训练结果”；若来源实验被删除，回退到该数据集的实验列表并提示。
- 多分类 Mock API 返回 `prediction`、`confidence`、按概率降序的完整 `probabilities` 和 `mock`。二分类响应补充 `positiveClass` 与 `threshold`。
- V9 使用 `mlStudioV4` 和 `schemaVersion: 4`，不读取、转换或删除旧的 `mlStudioV3` 测试状态。

### 3.2 V10.2.2 时序预测最终结论

- 项目创建增加“时序预测”；V1 只支持单实体、单序列，不支持门店 × 商品等多实体联合预测，也不包含时序分类、异常检测和不规则事件流。
- 数据集只选择一个时间列和一个数值目标列，可包含多个数值、类别或布尔外部特征，支持按日、按周、按月规则频率。系统先推断频率，再由用户确认。
- 数据检查提供与分类/回归一致的字段检查表和历史目标趋势图。无效日期、非数值目标、频率桶重复、历史时间缺口和历史长度不足会阻断；最低历史点为日 60、周 30、月 24。历史时间缺口不自动填补，需修复源数据后重新上传；原始行未排序时训练阶段自动排序并提示。
- 用户只设置未来预测期数；平台固定使用 3 轮扩展窗口滚动回测，默认按三轮平均 MAE 升序排名。上限为 `min(频率上限, floor((有效历史点 - 最低训练点) / 3))`，其中日/周/月频率上限分别为 90/26/12，越界实时标红并阻止生效。
- 特征准备包含日历特征总开关和上传外部特征筛选。自动筛选只使用历史缺失率（默认 90%）和归一化方差（默认 0.01）；时序相关性与最佳滞后只作参考。数值外部特征相关性热力图始终展开，默认在绝对相关性 0.8 时预警但不自动删除。所有指标只用首个回测训练窗口计算，避免验证期泄漏。
- 日历特征使用带“已开启/已关闭”文字的标准开关，并展示实际生成字段。阈值恢复不立即改变特征，只有点击“自动筛选”才批量更新；完成后展示纳入/排除数量，仍允许手动调整。
- 模型家族固定为基线预测、指数平滑、ARIMA/ARIMAX、Prophet、LightGBM 时序。基线与指数平滑仅使用目标历史；ARIMA 在纳入外部特征时展示为 ARIMAX；Prophet 和 LightGBM 可使用外部特征。
- 非基线模型展示模型专属常用参数、说明、范围和默认值恢复。调参方式统一为使用当前参数、快速自动调参和网格调参；自动调参只展示推荐范围，不展示搜索算法或次数；网格候选值使用英文逗号分隔，实时计算组合数并限制为最多 200 组。本轮不增加贝叶斯调参。
- 结果指标为 MAE、RMSE、sMAPE、稳定性，不展示 R²、MAPE 或分类指标。主表只展示三轮平均值，逐轮结果通过明细弹窗查看。
- 报告顺序为指标总览、数据与预测配置、滚动回测、误差分析、外部特征使用情况、模型说明、配置快照。使用外部特征的方案在尚未提交 API 未来特征前不编造未来预测图；仅 LightGBM 展示特征重要性。
- 数据集已有训练产物后，时间列、目标列和频率只能通过复制数据集修改；普通预测期数、日历特征或缺失策略变更会将已有实验标记为需要重新训练。
- 保存结果后必须进入对应模型实验的模型库详情。模型库保留跨数据集比较风险提示和配置差异提示，不给出跨配置全局最佳结论。
- 上传数据只包含历史记录，不包含未来目标或未来特征。Mock API 与已保存方案绑定；基线预测和指数平滑请求只包含 `horizon`，使用外部特征的 ARIMAX、Prophet 与 LightGBM 请求同时包含 `horizon` 和逐期 `futureFeatures`。类别和布尔特征使用可选值控件。响应包含未来日期、预测值和 80%/95% 区间；不设计真实服务、认证、调用额度或在线更新。
- 内置“商品每日销量示例”：365 期完整历史数据，一个销量目标与商品价格、是否促销、广告费用、节假日类型四个外部字段，默认预测未来 7 期、日历特征开启。

## 4. 已完成页面与组件

### 4.1 项目与数据集

- 项目列表、项目创建、项目详情、创建时间展示。
- 项目可直接创建；目标列不在项目创建时选择。
- 添加数据集时选择 CSV 或与项目任务匹配的示例数据。
- 提供二分类、多分类和回归示例；多分类内置“客户价值等级预测”（4 类）和“贷款风险等级预测”（3 类），覆盖数值、类别、布尔、缺失、日期、ID、低方差、高相关和不平衡场景。
- 数据集卡片有两个独立入口：“进入数据集”和“模型实验（数量）”。
- 数据集悬浮弹窗包含数据概览和数据预览。
- 数据概览最多前 50 个特征，每页 10 个；数据预览有表头和分页。
- CSV 字段类型检查、列内类型一致性展示、目标列设置、不可训练字段标记。
- 项目、数据集和模型实验支持删除及级联清理。

### 4.2 特征准备

- 固定流程：`数据检查 → 特征准备 → 模型训练 → 训练结果`。
- 特征准备包含训练集/验证集划分和特征管理。
- 默认比例 `80% / 20%`，保留其他预设比例；二分类和多分类均使用分层划分。
- 自动筛选与“推荐阈值”是两个不同功能。
- 自动筛选使用系统固定推荐规则，直接更新特征列表。
- 推荐阈值只恢复输入框数字，用户仍可修改，再点击“确定修改”。
- 默认筛选：缺失率不高于 90%；二分类 IV 不低于 0.01；多分类数值特征归一化方差不低于 0.01；另有 PSI、低方差、目标相关性规则。
- 相关性矩阵使用热力图；阈值和相关性自动处理都位于热力图区块。
- 高相关处理先展示动态预览，确认后排除，且允许用户重新纳入。
- IV、PSI、低方差和目标相关性有简短说明；缺失率和特征间相关性不增加解释区间。

### 4.3 模型训练与调参

- 分类模型：逻辑回归、决策树、随机森林、KNN、LGBM。
- 回归模型：线性回归、回归树、随机森林回归、LGBM 回归。
- 模型只展示适用的参数和调参方式。
- 常用参数精简展示，并提供简短解释和推荐值。
- 高级参数默认折叠；枚举参数使用下拉框，例如划分标准、求解器、最大特征数。
- 网格调参使用预设候选值，但高级设置允许修改，并实时计算组合数量。
- 快速自动调参不展示算法名称、尝试次数或随机种子，只保留最佳三组结果。
- 训练进度为不可通过遮罩关闭的居中弹窗，支持最小化和取消。
- 成功实验锁定，不能再次训练；需要修改时复制为新模型实验。
- 已完成实验在列表和详情中都可直接进入训练结果。

### 4.4 训练结果与报告

- 结果默认展示 Top 3，可展开全部结果。
- 默认仅勾选当前排序指标下第一名，用户可以多选其他结果。
- 结果可按训练集或验证集指标排序，默认验证集。
- 二分类、多分类和回归的“显示指标”使用各自独立状态；复选项点击后立即更新，面板保持展开，至少保留一项，取消当前排序指标后自动切换到剩余第一项。
- 分类指标：AUC、KS、F1、准确率、精确率、召回率、训练/验证差值。
- 回归指标：RMSE、MAE、R²、训练/验证差值。
- 模型报告是训练结果下级详情页，不是第五个流程步骤。
- 分类报告包含 ROC、KS、Gain Table、验证集混淆矩阵及 Accuracy、Precision、Recall、F1 计算。
- 报告展示训练集指标、验证集指标和差值。
- 变量重要性明确标注计算方式，不跨模型直接比较分数。

### 4.5 模型库与 API 演示

- 模型库是全局一级页面，可查看全部项目。
- 支持项目、数据集、模型类型筛选和指标排序。
- 同一实验保存多组结果时，每组形成独立模型库记录，但不创建新实验。
- 保存后先写入本地模型库，再跳转，确保新增结果立即可见。
- 项目级详情在数据集、模型实验、模型类型表头提供联动筛选；数据集级详情在模型实验、模型类型表头提供联动筛选。筛选区不单独占据表格上方空间，范围列、参数方案、指标和创建时间均可由表头排序。
- 模型库详情增加指标列后只在表格内部横向滚动，不允许撑宽整个页面；训练结果和模型库的指标列按内容及可用空间自动分配，达到最小可读宽度后使用内部滚动。
- 模型报告不维护独立保存状态；模型库打开报告时读取已保存参数方案的不可变结果快照，导出仍使用浏览器打印并由用户选择另存为 PDF。
- 模型库展示项目、数据集、实验、模型类型、参数、训练/验证指标和创建时间。
- 重复保存会阻止新增，并提示结果已保存。
- API 服务属于模型部署概念，但当前只提供前端模拟接入配置，不是真实部署。

### 4.6 导航与状态

- 一级页面不显示返回按钮。
- 所有二级页面使用统一底部固定操作栏：返回在左，下一步或主要操作在右。
- 下一步按钮必须位于右下方或底部吸附区，不能移动到页面顶部。
- 返回时保留已填写配置、划分比例和筛选状态。
- 演示数据和操作状态使用 `LocalStorage` 保存。

## 5. 已确认的关键设计决策

- 彻底取消机器学习流程画布，不做节点拖拽、连线、缩放或自由编排。
- 用户通过固定向导式流程操作；“零代码”不等于必须使用画布。
- 项目创建不弹出目标列选择；创建数据集时再选择目标列。
- 字段类型检查属于数据集模块；自动排除属于特征管理。
- 不可训练字段必须明确标注“不可用于训练”，而不是弱提示“建议排除”。
- 全平台使用验证集，不使用“测试集”命名，也不使用交叉验证。
- 缺失值填充、编码和标准化参数只根据训练集计算，再应用到验证集，防止数据泄漏。
- 特征间相关系数绝对值越高，冗余风险越大；不是绝对值越低就一定越好。
- 类别缺失值的“不处理”和“作为独立类别”含义不同；当前允许模型采用适合自身的默认方式。
- 一个成功实验不能重复训练；修改配置必须复制为新模型实验。
- 快速自动调参只保留最佳三组；结果页默认展示 Top 3，但只默认勾选当前第一名。
- 模型库保存的是实验下的参数结果，不把每组参数误称为新实验。

## 6. 用户曾纠正的错误与防回退要求

- 曾遗漏 V6 中的底部下一步按钮和训练进度弹窗；后续修改必须完整继承最新需求，不能只实现新增六项。
- 曾把返回按钮放在不同页面的不同位置；现在必须统一到底部固定操作栏。
- 曾把模型报告放进主流程；现在报告只能是训练结果详情页。
- 曾使用“测试集”和交叉验证；最终规则是训练集/验证集，不使用交叉验证。
- 曾讨论按全集计算预处理参数；最终用户接受防泄漏方案，只按训练集计算。
- 曾把“自动筛选”和“推荐阈值”混为同一按钮；两个按钮的作用必须保持独立。
- 曾使用“建议排除”等较弱措辞；不可训练字段必须明确显示“不可用于训练”。
- 曾默认勾选 Top 3；最终规则是默认只勾选当前排序第一名，可以手动多选。
- 曾在保存后只跳转模型库但没有写入模型；现在必须先保存数据，再跳转并立即展示。
- 曾缺少分类混淆矩阵及指标计算；现在验证集报告必须保留这些内容。
- 曾遗漏分类/回归两类示例或特征过少；现在两类示例都必须存在并具有较多特征。
- 用户多次强调“不要增加不必要的改动”，禁止借需求修改进行整体视觉重做或无关重构。

## 7. 禁止修改或恢复的内容

- 禁止恢复自由画布、拖拽建模流程、节点和连线。
- 禁止把目标列选择移动到项目创建流程。
- 禁止把“验证集”改回“测试集”，或自行加入交叉验证。
- 禁止按完整数据集计算填充、编码和标准化参数。
- 禁止展示随机种子、自动调参算法名称或尝试次数。
- 禁止移动底部固定返回/下一步操作栏。
- 禁止把模型报告重新加入主步骤条。
- 禁止默认勾选前三名结果。
- 禁止把同一实验的多组参数结果创建为多个实验。
- 禁止新增真实 API、账号权限、云存储或其他未确认模块。
- V9.1.0 已获用户批准整理失效覆盖层；仍保持原生 JavaScript/CSS、单入口和直接打开能力，不引入 React 或构建工具。

## 8. 当前问题与已知缺口

当前没有阻塞前端继续评审的问题，页面可直接运行。以下是已知限制，不能在未确认时擅自扩展：

- 所有训练、指标、统计数据和 API 均为 Mock，不连接真实机器学习服务。
- CSV 完整数据不会写入 LocalStorage；仅保留前 150 行预览和字段统计，页面每页展示 30 行。
- 训练流程目前只有成功和取消演示，尚未提供可主动触发的失败场景；需求文档要求失败时展示原因和返回修改入口。
- 混淆矩阵 Mock 数据通常不会出现分母为 0；开发文档规定真实数据下应显示 `—`，不能显示 `NaN`。
- 模型库详情可直接打开对应已保存参数方案的模型报告，也可返回来源训练结果。
- 高级参数控件以界面演示为主，并非所有输入都会形成真实训练请求。
- V3–V9 仍有历史覆盖层，V9.1.0 已删除被 PDF 导出替代的报告保存覆盖并建立统一最终入口；后续应在逐流程回归后继续清理，不建议一次性重写。

## 9. 运行与验证

在项目目录启动静态服务器：

```powershell
python -m http.server 4173 --bind 127.0.0.1
```

浏览器打开 `http://127.0.0.1:4173/`。

运行无依赖回归检查：

```powershell
node qa/smoke.test.js
```

V10.2.2 自动回归已验证：

- CSV 引号、字段内逗号、字段内换行、转义引号和未闭合引号处理。
- 模拟训练结果包含固定来源与生成时间。
- 回归目标列数值校验。
- 数据预览每页 30 行。
- Hash 路由生成、无效对象降级和直接恢复。
- 模型训练快照包含目标列和入模特征。
- 首次保存与重复保存训练结果均进入对应实验的模型库详情。
- 模型库进入报告后可返回原模型库范围，模型库范围切换同步 Hash。
- 无训练结果时，步骤条禁用训练结果和模型报告入口。
- 时序示例数据包含 365 期完整历史记录、1 个目标与 4 个外部字段且无阻断项。
- 时序模型稳定生成 3 轮回测和 7 期未来预测；报告包含原生 SVG 与 95% 预测区间。
- 外部特征筛选页自动指标仅含历史缺失率和归一化方差；时序相关性仅供参考，相关性热力图始终展开。
- 时序结果与模型库表格使用内部横向滚动、自动指标列宽和固定横向操作列。
- 时序结果保存后进入模型库实验详情，报告始终读取保存时的不可变结果快照。
- 使用外部特征的时序方案 API 请求包含逐期 `futureFeatures`，编辑输入后稳定 Mock 响应同步变化；基线预测请求不包含外部特征。

当前宿主的浏览器连接仍受 Windows 沙箱初始化故障影响，V10.2.2 的点击和视觉回归尚需在恢复连接后补做；不得把隔离 DOM 自动回归等同于浏览器实测。

## 10. 后续修改流程

1. 先阅读 `frontend-change-requirements-v7.md` 和本文件。
2. 对照 `机器学习训练平台前端开发文档.md` 判断是否需要同步更新文档。
3. 只修改用户指定范围；脚本统一修改 `app.js`，样式统一修改 `app.css`，不要再创建按版本递增的脚本或样式文件。
4. 回归分类和回归两条主流程，特别检查底部按钮、训练弹窗、结果保存和模型库。
5. 修改完成后执行 JavaScript 语法检查和浏览器控制台检查。
