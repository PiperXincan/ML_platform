# 机器学习平台前端修改需求 V4

## 1. 文档目的

本文档记录《机器学习平台待修改需求v4.docx》及后续产品讨论确认的全部修改，作为升级前端开发文档和网页的直接依据。

当前阶段仍只实现桌面 Web 前端。训练、调参、报告和 API 接入配置均使用稳定 Mock 数据，不执行真实模型训练或部署。

## 2. 项目与数据集创建

### 2.1 项目创建

- 创建项目时填写项目名称和任务类型，系统自动记录创建时间。
- 项目可以在没有数据集和目标列的情况下直接创建。
- 项目上传数据后，任务类型不可修改。
- 目标列不属于项目，不在创建项目时选择。

### 2.2 数据集创建

- 点击“上传数据集”后打开统一弹窗。
- 弹窗提供“上传 CSV”和“使用示例数据”两个入口。
- 示例数据只展示符合当前项目任务类型的数据集。
- 选择数据后进入数据集创建页面，确认数据集名称、字段类型、目标列和正类。
- 每个数据集分别保存自己的目标列。

## 3. 数据集悬浮弹窗

- 取消项目名称上的悬浮弹窗。
- 鼠标停留数据集卡片约 300ms 后打开数据集弹窗。
- 鼠标进入弹窗后保持显示，右上角提供“固定”按钮。
- 固定后鼠标移出不关闭，点击页面空白处关闭。
- 键盘聚焦数据集名称时也可以打开弹窗。
- 点击数据集名称进入完整数据检查页面。

弹窗包含两个分页：

### 3.1 数据概览

- 行数、列数、目标列和缺失值数量。
- 数值字段的均值、中位数、最小值和最大值摘要。
- 前 5 行数据预览。

### 3.2 数据预览

- 最多读取前 150 行。
- 每页 20 行。
- 支持分页和横向滚动。

统计指标必须基于完整数据集计算，不能只使用前 150 行。页面明确标注：“统计指标基于完整数据集，表格仅预览前 150 行。”

## 4. 固定流程调整

固定流程修改为：

`数据检查 → 特征准备 → 模型训练 → 调参结果 → 模型报告`

### 4.1 特征准备

特征准备只包含：

1. 训练集 / 测试集划分
2. 特征管理

- 同一数据集下的模型实验共享数据划分和特征集合。
- 修改后，已有成功实验变为历史结果，不能与新实验默认混合比较。
- 特征准备页面名称不使用“数据准备”或“训练数据设置”。

### 4.2 模型训练

每个模型实验独立配置：

- 缺失值处理
- 标准化
- 模型参数
- 调参方式

不同模型可以使用不同预处理配置，但必须共享相同的数据划分和随机种子。

## 5. 数据划分与防止数据泄漏

### 5.1 数据划分顺序

特征准备页面内部顺序为：

1. 设置训练集 / 测试集比例。
2. 固定随机种子并完成划分。
3. 仅使用训练集计算特征筛选指标。
4. 将相同特征规则应用到测试集。

- 特征筛选页面标注“指标根据训练集计算”。
- 缺失率、特征间相关性、IV、PSI、低方差和目标相关性默认基于训练集展示。

### 5.2 预处理参数

- 均值、中位数和众数只根据训练集计算。
- 标准化的均值和标准差只根据训练集计算。
- 使用训练集得到的参数转换测试集。
- 删除缺失行时，训练集和测试集分别处理。
- 模型报告注明“所有预处理参数仅由训练集计算”。

### 5.3 调参与测试集

- 用户仍只设置训练集 / 测试集比例。
- 调参只在训练集内部进行交叉验证。
- 调参结果展示交叉验证指标。
- 测试集不参与参数排序和 Top 3 选择。
- 参数方案确定后，测试集只用于一次最终模型评估。
- 模型报告展示测试集指标。

### 5.4 二分类分层划分

- 二分类默认使用分层划分，保持训练集和测试集类别比例接近。
- 数据检查页展示目标类别分布和正类占比。
- 正类占比过低时显示“类别不平衡”提示。
- 准确率保留，但提示“不平衡数据中不能单独判断模型好坏”。
- 当前不增加过采样、欠采样和类别权重功能。

## 6. 特征筛选指标

### 6.1 二分类

- 缺失率
- IV
- PSI
- 特征间相关性

### 6.2 回归

- 缺失率
- 低方差
- 与目标变量的相关性
- 特征间相关性
- PSI

回归任务不展示 IV，也不使用 `IV ≥ 0.01` 的规则。

## 7. 缺失值处理

### 7.1 数值字段

- 不处理
- 均值填充
- 中位数填充
- 众数填充
- 固定值填充
- 删除所在行

### 7.2 类别字段

- 不处理
- 保留为缺失类别
- 众数填充
- 固定值填充
- 删除所在行

### 7.3 模型默认规则

| 模型 | 数值字段默认 | 类别字段默认 |
| --- | --- | --- |
| 逻辑回归、线性回归 | 均值填充 | 保留为缺失类别 |
| KNN | 中位数填充 | 保留为缺失类别 |
| 决策树、随机森林 | 中位数填充 | 保留为缺失类别 |
| LGBM | 不处理 | 不处理 |

- 用户可以逐字段修改，也可以批量应用。
- 对可能导致训练失败的“不处理”设置显示警告，但允许用户选择。

## 8. 标准化

- 逻辑回归、KNN 和线性回归默认开启。
- 决策树、随机森林和 LGBM 默认关闭。
- 用户可以人工修改。
- 不推荐配置只提示原因，不阻止训练。
- 标准化属于模型实验配置。

## 9. 模型参数

### 9.1 默认展示参数

| 模型 | 参数 |
| --- | --- |
| 逻辑回归 | 正则化强度 `C` |
| 决策树分类 / 回归 | 最大深度、最小叶节点样本数 |
| 随机森林分类 / 回归 | 树数量、最大深度 |
| KNN 分类 | 邻居数量 |
| LGBM 分类 / 回归 | 树数量、学习率、叶子数 |
| 线性回归 | 不展示可调参数 |

- 参数旁提供简短解释和推荐默认值。
- 默认区只展示最常用参数。
- 提供折叠的“高级参数设置”，允许配置更多参数。
- 高级参数提供解释、默认值、范围校验和恢复默认值。
- 调参结果记录实际使用的全部参数。

## 10. 模型实验生命周期

- 一个模型实验成功后只能训练一次。
- 草稿、训练失败和已取消实验允许修改并重新训练。
- 训练成功后配置和结果锁定。
- 需要调整成功实验时，使用“复制为新模型实验”。
- 复制后的名称自动递增。
- 草稿和失败实验可以删除；成功实验只能归档。
- 上游配置变化后，成功实验保留为历史结果，不能重新训练或生成新的 API 接入配置。

## 11. 调参结果

- 调参结果使用表格，不使用卡片。
- 默认展示综合效果最好的 Top 3。
- “查看全部结果”展开完整表格。
- 页面顶部提供排序指标下拉框。

### 11.1 分类排序

- 默认按交叉验证 AUC 降序。
- 可切换 KS、F1、准确率、精确率、召回率和训练 / 验证差值。

### 11.2 回归排序

- 默认按交叉验证 RMSE 升序。
- 可切换 MAE、R² 和训练 / 验证差值。

- 指标表头提供简短解释和“越大越好 / 越小越好”。
- 每行提供“设为主方案”和“查看模型报告”。
- “查看模型报告”直接打开该参数方案对应的报告，不增加中间详情页。
- 测试集指标不用于调参排序。

## 12. 调参方案保存

- 调参结束后进入“保存到模型库”选择步骤。
- 默认勾选 Top 3。
- 用户可以取消勾选或选择更多结果，至少保存一组。
- 用户指定其中一组为主方案，默认 Top 1。
- Top 3 是同一个模型实验下的参数方案，不是三个独立模型。
- 模型库一级记录仍然只有一个模型实验。

## 13. 模型报告

- 调参结果中的“查看模型报告”直接进入对应参数方案报告。
- 模型报告使用测试集进行最终评估。
- 明确标注“所有预处理参数仅由训练集计算”。
- 分类报告继续展示 AUC、KS、F1、精确率、召回率、ROC、KS 曲线、混淆矩阵和 Gain Table。
- 回归报告展示 RMSE、MAE、R²、预测值对比和残差分布。

Lift 简短解释统一为：

> Lift：模型筛选效果相对随机筛选提升了多少倍，越大越好。

示例说明：

> Lift = 3，表示命中率约为随机筛选的 3 倍。

## 14. 模型库

模型库采用分层结构：

`项目 → 数据集 → 模型实验 → 已保存参数方案`

### 14.1 模型比较

- 先选择项目和数据集。
- 默认展示该数据集下所有模型类型。
- 支持按模型类型多选筛选。
- 同一模型类型的多个实验分别显示，不自动合并。
- 默认只比较各实验主方案。
- 提供“显示全部已保存方案”开关。
- 排序指标和方向与调参结果页面一致。
- 历史结果、需要重新训练和失败模型保留展示，但不参与默认排序。
- 每个方案可以返回来源模型实验查看完整调参结果。
- 页面标注：“模型指标包含数据预处理方式带来的影响。”

### 14.2 模型入库

- 普通训练成功后自动保存唯一结果。
- 调参实验根据用户勾选保存参数方案，默认 Top 3。
- 草稿、训练中和已取消实验不进入模型库。

## 15. API 接入配置

- API 不属于训练主流程，只放在模型库的更多操作中。
- 当前功能名称使用“生成 API 接入配置”，不使用“部署为 API”。
- 进入前展示说明：“API 用于让其他系统调用模型预测结果。”
- 明确标注“模拟 API 接入配置，不可用于真实业务调用”。
- 只能从有效、已完成、已保存的参数方案生成。
- 生成时可以选择任一已保存参数方案，默认主方案。
- API 配置绑定具体参数方案。
- 修改主方案不会自动改变已有 API 配置。

### 15.1 API 配置管理

模型库增加“模型比较”和“API 服务”两个页签。

API 服务列表展示：

- API 服务名称
- 来源项目、数据集、模型实验和参数方案
- 模拟接口地址
- 创建时间
- 当前状态

支持操作：

- 查看接口说明
- 复制接口地址
- 查看 Python、JavaScript 和 cURL 示例
- 发送模拟测试请求
- 更新模型方案
- 删除 API 接入配置

不增加启用 / 停用、密钥、权限、调用统计、额度和版本回滚。

## 16. 非必要功能限制

- 不实现真实模型训练、部署和预测 API。
- 不实现多次训练历史；成功实验必须复制为新实验。
- 不实现采样平衡、类别权重和复杂数据版本管理。
- 不实现项目悬浮弹窗。
- 不实现移动端和平板适配。
