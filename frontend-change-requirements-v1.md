# 机器学习平台前端修改需求 V1

## 1. 文档目的

本文档记录机器学习平台试用后确认的前端修改需求，作为后续修改网页、补充交互和验收功能的依据。

当前阶段仍然只完成前端。缺失率、IV、PSI、相关系数、训练结果和调参结果均使用稳定的模拟数据展示，不接入真实机器学习后端。

## 2. 修改范围概览

本次修改包括：

1. 支持用户手动排除不参与训练的特征。
2. 增加缺失率、IV、PSI、特征间相关性等特征筛选指标。
3. 支持修改特征筛选阈值。
4. 对高相关特征提供自动保留建议。
5. 训练集与测试集支持预设比例选择。
6. 工作流画布支持缩放、平移和适应画布。
7. 完善模型参数配置。
8. 增加 LGBM 分类模型和 LGBM 回归模型。
9. 增加网格调参与贝叶斯调参。
10. 增加调参结果总览、排序和人工选择。
11. 模型报告增加描述性统计。
12. 模型报告展示 Top 10 变量重要性。

## 3. 前端实现边界

- 保持纯前端实现，不调用真实训练服务。
- 所有指标由前端 Mock Service 根据数据集、特征、模型和参数生成。
- 相同输入应生成稳定结果，刷新页面后不能随机大幅变化。
- 界面应明确提示“当前结果为模拟结果”。
- 页面和状态结构需为未来接入真实 API 预留服务层接口。

## 4. 特征管理

### 4.1 功能入口

在数据集页面新增“特征管理”区域，也可以在工作流组件库中增加“特征筛选”节点。用户上传 CSV、完成字段类型检查并选择目标列后，进入特征管理。

目标列不参与特征筛选，不出现在候选特征列表中。

### 4.2 特征列表

每个候选特征展示：

| 字段 | 说明 |
| --- | --- |
| 是否纳入训练 | 用户可手动启用或排除特征 |
| 特征名称 | CSV 字段名称 |
| 数据类型 | 数值、类别、日期、布尔或文本 |
| 缺失率 | 当前特征缺失值占比 |
| IV | 模拟的信息价值指标 |
| PSI | 模拟的分布稳定性指标 |
| 筛选状态 | 保留、建议排除、已手动排除或稳定性风险 |
| 原因 | 显示触发的规则和建议动作 |

### 4.3 手动排除

- 用户可以指定任意特征不参与训练。
- 被排除的特征保留在列表中，但状态显示为“已手动排除”。
- 用户可以恢复已排除的特征。
- 工作流校验和模型报告只使用最终纳入训练的特征。
- 至少保留一个可用特征，否则不允许开始训练。

## 5. 特征筛选规则

### 5.1 默认阈值

| 指标 | 默认阈值 | 判断规则 |
| --- | --- | --- |
| 缺失率 | 90% | 缺失率高于 90% 时建议排除 |
| IV | 0.01 | IV 低于 0.01 时建议排除 |
| PSI | 0.25 | PSI 高于 0.25 时提示稳定性风险，但不强制排除 |
| 特征间相关系数 | 0.8 | 两个特征的相关系数绝对值高于 0.8 时提示高共线性风险 |

### 5.2 阈值配置

- 用户可在特征管理区域修改缺失率、IV、PSI 和相关系数阈值。
- 修改阈值后，列表中的推荐状态实时重新计算。
- 提供“恢复默认阈值”按钮。
- 阈值输入需限制在合理范围，并显示单位和含义。

### 5.3 PSI 处理

- PSI 用于比较训练集与验证集或测试集之间的特征分布稳定性。
- PSI 超过默认阈值 `0.25` 时显示风险提示。
- PSI 超标不自动删除特征，由用户决定是否排除。

### 5.4 特征间相关性

本需求中的相关系数指特征与特征之间的相关性，不是特征与目标变量之间的相关性。

- 计算并模拟展示数值特征两两相关系数。
- 相关系数使用绝对值判断，绝对值越高表示特征信息越重复。
- 当绝对相关系数高于默认阈值 `0.8` 时，标记为高共线性风险。
- 对高相关特征对，系统自动推荐保留其中一个特征。
- 推荐优先级为：优先保留 IV 更高的特征；IV 接近时优先保留缺失率更低的特征。
- 用户可以覆盖系统推荐，手动选择保留哪一个。
- 页面可使用“高相关特征对”列表或相关性矩阵展示风险。

## 6. 训练集与测试集划分

训练/测试集划分节点仅提供预设比例，不开放任意比例输入。

可选比例：

- `70% / 30%`
- `75% / 25%`
- `80% / 20%`，默认选项
- `90% / 10%`

选择后需要同步更新节点摘要、训练配置和模型报告。

## 7. 工作流画布

### 7.1 必要操作

- 节点可拖动调整位置。
- 用户可以拖动画布空白区域进行平移。
- 提供“放大”按钮。
- 提供“缩小”按钮。
- 提供“适应画布”按钮，使全部节点自动进入可视区域。
- 缩放与平移不能影响节点连接关系和保存结果。

### 7.2 画布状态保存

- 保存每个节点的位置。
- 保存画布缩放比例和平移位置。
- 刷新页面或重新打开项目后恢复之前的画布状态。

## 8. 模型清单

### 8.1 二分类模型

- 逻辑回归
- 决策树分类
- 随机森林分类
- KNN 分类
- LGBM 分类

### 8.2 回归模型

- 线性回归
- 决策树回归
- 随机森林回归
- LGBM 回归

## 9. 模型参数

模型配置面板默认展示核心参数，其余参数放入“高级设置”。

| 模型 | 默认展示参数 |
| --- | --- |
| 逻辑回归 | 正则化强度 |
| 决策树分类/回归 | 最大深度、最小叶节点样本数 |
| 随机森林分类/回归 | 树数量、最大深度 |
| KNN 分类 | 邻居数量 |
| 线性回归 | 是否使用正则化 |
| LGBM 分类/回归 | 树数量、学习率、叶子数、最大深度 |

所有模型统一提供随机种子设置。训练/测试比例由数据划分节点负责，不在模型节点中重复配置。

## 10. 调参功能

### 10.1 调参方式

模型节点提供三种训练方式：

1. 不调参：直接使用当前参数训练。
2. 网格调参：遍历预设候选参数组合。
3. 贝叶斯调参：根据最大尝试次数生成模拟搜索结果。

LGBM 分类和 LGBM 回归必须同时支持网格调参与贝叶斯调参。

### 10.2 网格调参配置

- 参数范围使用预设候选值，不允许用户手写复杂表达式。
- 每个参数通过多选控件选择候选值。
- 需要实时显示将生成的参数组合数量。
- 参数组合过多时给出提醒，但前端模拟不执行真实计算。

LGBM 示例候选值：

| 参数 | 示例候选值 |
| --- | --- |
| 树数量 | 100、200、300 |
| 学习率 | 0.01、0.05、0.1 |
| 叶子数 | 15、31、63 |
| 最大深度 | 5、8、12 |

### 10.3 贝叶斯调参配置

- 用户只需设置最大尝试次数。
- 前端以模拟方式逐次显示搜索进度和候选参数。
- 结果页与网格调参共用同一套总览表。

## 11. 调参结果总览

### 11.1 总览表字段

每一行代表一组候选参数，展示：

- 序号；
- 主要参数值；
- 训练集 AUC；
- 验证集 AUC；
- 训练集与验证集 AUC 差值；
- KS；
- 运行状态；
- 是否为当前选中方案。

回归任务使用对应的回归指标替代 AUC 和 KS，例如训练集与验证集的 RMSE、MAE、R²。

### 11.2 排序

- 二分类结果默认按验证集 AUC 从高到低排序。
- 支持按训练集与验证集 AUC 差值排序，用于识别过拟合风险。
- 差值默认按绝对值从低到高排序。
- 用户点击列标题即可切换排序。

### 11.3 人工选择

- 用户可以点击任意一行查看完整参数。
- 用户点击“设为当前方案”后，将该参数组标记为最终模型方案。
- 选定参数后进入对应的模型报告。
- 自动推荐结果只作为建议，最终选择由用户决定。

## 12. 模型报告

### 12.1 原有内容

二分类继续展示准确率、AUC、KS、ROC 曲线、KS 曲线、混淆矩阵、Gain Table、精确率、召回率和 F1。

回归继续展示 MAE、RMSE、R²、真实值与预测值对比图和残差图。

### 12.2 数据描述性统计

模型报告增加“数据概览”区域，只统计最终参与训练的特征，不展示被排除字段。

数值特征展示：

- 样本数；
- 缺失率；
- 均值；
- 中位数；
- 标准差；
- 最小值；
- 最大值。

类别特征展示：

- 样本数；
- 缺失率；
- 唯一值数量。

类别特征不展示众数。

### 12.3 变量重要性

- 模型报告固定展示变量重要性 Top 10。
- 使用横向条形图展示。
- 同时展示排名、特征名称和重要性得分。
- 如果最终特征不足 10 个，则展示全部特征。
- 已排除特征不能出现在变量重要性中。

## 13. Mock 数据要求

- 缺失率、IV、PSI、相关性、调参结果和模型报告均由前端生成模拟数据。
- 模拟数据需要符合基本逻辑，例如 IV 较高的特征不应总是重要性最低。
- 高相关特征推荐结果应与 IV 和缺失率规则一致。
- 同一项目、数据集和配置应使用稳定种子生成相同结果。
- 修改筛选阈值、排除特征、划分比例或参数后，相关页面应同步更新。

## 14. 页面改动清单

| 页面 | 修改内容 |
| --- | --- |
| 数据集页 | 增加特征管理、指标列、筛选阈值和手动排除 |
| 工作流编辑器 | 增加特征筛选节点、画布缩放/平移/适应画布、LGBM 节点 |
| 模型属性面板 | 增加核心参数、调参方式和候选值配置 |
| 训练控制台 | 调参时展示候选组合和尝试进度 |
| 调参结果页 | 增加参数总览、排序、方案选择和模型报告入口 |
| 模型报告 | 增加描述性统计与 Top 10 变量重要性 |

## 15. 建议前端数据结构

```ts
interface FeatureMetric {
  featureName: string;
  dataType: 'number' | 'category' | 'date' | 'boolean' | 'text';
  included: boolean;
  exclusionSource?: 'manual' | 'recommended';
  missingRate: number;
  iv: number;
  psi: number;
  status: 'keep' | 'exclude-recommended' | 'stability-risk' | 'manual-excluded';
  reasons: string[];
}

interface CorrelationPair {
  firstFeature: string;
  secondFeature: string;
  correlation: number;
  recommendedKeep: string;
  recommendedExclude: string;
  reason: string;
}

interface FeatureThresholds {
  maximumMissingRate: number;
  minimumIv: number;
  maximumPsi: number;
  maximumAbsoluteCorrelation: number;
}

type SplitPreset = '70-30' | '75-25' | '80-20' | '90-10';
type TuningMethod = 'none' | 'grid' | 'bayesian';

interface TuningConfig {
  method: TuningMethod;
  candidateValues?: Record<string, Array<number | string>>;
  maximumTrials?: number;
}

interface TuningTrial {
  id: string;
  parameters: Record<string, number | string | boolean>;
  trainingMetrics: Record<string, number>;
  validationMetrics: Record<string, number>;
  metricGap: number;
  selected: boolean;
}
```

## 16. 验收标准

- 用户可以手动排除并恢复特征。
- 页面默认使用缺失率 `90%`、IV `0.01`、PSI `0.25`、相关系数绝对值 `0.8` 的阈值。
- 修改阈值后，特征推荐状态立即更新。
- 高相关特征对能够根据 IV 和缺失率给出自动保留建议。
- 用户可以覆盖自动推荐。
- 数据划分节点仅提供四种预设比例，默认 `80% / 20%`。
- 工作流画布可以平移、放大、缩小和适应画布。
- 分类与回归模型列表均包含 LGBM。
- LGBM 支持网格调参与贝叶斯调参配置。
- 网格调参使用预设候选值并显示组合数量。
- 二分类调参结果默认按验证集 AUC 降序排列。
- 调参结果可按训练集和验证集 AUC 差值排序。
- 用户可人工选择某组参数并查看模型报告。
- 模型报告包含最终训练特征的描述性统计。
- 类别特征描述性统计不展示众数。
- 模型报告展示变量重要性 Top 10。
- 所有新增功能在没有后端的情况下可以使用稳定 Mock 数据完成演示。

